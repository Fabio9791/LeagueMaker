{% extends 'base.html.twig' %}

{% block body %}

	<table class="table">
		<thead>
			<tr>
				<th>Tag Name</th>
			</tr>
		</thead>
		<tbody>
			{% for tag in tags %}
    			<tr>
    				<td>{{ tag.getLabel() }}</td>
    			</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}